import React from "react";
import { StyleSheet, Text, View } from "react-native";

export const SearchVideoScreen = () => {
  return (
    <View>
      <Text>Search Video Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
